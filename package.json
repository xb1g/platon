{
  "name": "agent-memory-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo bootstrap for a developer-facing memory platform for AI agent businesses.",
  "packageManager": "pnpm@10.30.2",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "check:workspace": "pnpm -r exec pwd"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
